# Portfolio & Blog Project Guide

تم تحويل المشروع بنجاح إلى تطبيق Laravel متكامل! 🚀

## 🏁 كيفية التشغيل

1. **تشغيل السيرفر**:
   افتح التيرمينال واكتب:
   ```bash
   php artisan serve
   ```

2. **الدخول للوحة التحكم**:
   - الرابط: `http://localhost:8000/admin/posts`
   - البريد: `admin@albalahy4u.com`
   - كلمة المرور: `password`

3. **مشاهدة الموقع**:
   - الرئيسية: `http://localhost:8000`
   - المدونة: `http://localhost:8000/blog`

## ✨ المميزات الجديدة

- **لوحة تحكم (Dashboard)**: لإدارة المقالات (إضافة، تعديل، حذف).
- **محرر نصوص ذكي**: (TinyMCE) يدعم تنسيق النصوص، الصور، والروابط.
- **قاعدة بيانات MySQL**: لحفظ البيانات بشكل دائم.
- **تجهيز الذكاء الاصطناعي**: تم إنشاء `AiService` لربطه لاحقاً بـ OpenAI.

## ⚠️ ملاحظات هامة

- تم إعداد ملفات `tailwind.config.cjs` لضمان عمل التصميم بشكل صحيح.
- لا تنس تغيير كلمة مرور الأدمن بعد الدخول الأول.
- لدمج AI بشكل كامل، سنحتاج لاحقاً لإضافة API Key في ملف `.env`.
